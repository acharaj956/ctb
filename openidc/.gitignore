# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Compiled C kernel (built inside the image; built locally only for tests)
*.so
*.o

# PKI material generated at runtime (never commit private keys)
pki/
*.pem

# Editor / OS
.DS_Store
.idea/
.vscode/
