# Auto-provision the Prometheus datasource so Grafana works on first start.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: openidc-prom
    isDefault: true
    editable: true
